## App updates

1. I want a sprint for user preferences
2. I want to watch a directory and load all the logs in the directory (and sub directories)
3. We don't need the log levels in the sidebar and the nav bar - in fact the whole UI needs an enema
4. Add a 'recently opened' section to the `File` menu