## App updates

1. I want to watch a directory and load all the logs in the directory (and subdirectories)
2. More importantly, I want to coalesce archived logs into a single file   
3. Can I coalesce multiple lines into a single line (1 of xxx)?
4. Add GitHub actions to build and deploy the app
5. Add a user 'save' option to save and load the current setup (log files in a window)
6. UI Tests? With screenshots?
7. Add keyboard shortcuts for common actions (Cmd+W for close on Mac)


   
