## App updates

1. I want to watch a directory and load all the logs in the directory (and subdirectories)
   2. For this I want options, interleaave (the are different log sources), combine (single log source, multiple 
      files) or split
2. More importantly, I want to coalesce archived logs into a single file   
3. Can I coalesce multiple lines into a single line (1 of xxx)?
4. [x] Add GitHub actions to build and deploy the app
5. Add a user 'save' option to save and load the current setup (log files in a window)
6. UI Tests? With screenshots? Can I automate this with Junie?
7. Are there standard endpoints for log files?
8. Add keyboard shortcuts for common actions (Cmd+W for close on Mac)
9. Can I write an MCP server for the app?
10. Change the list of logs in a window to a dropdown so that I can select the logs to view and delete logs from 
    that view
11. Highlight lines in the log and email them
12. Add a feature to filter logs by timestamp


   
