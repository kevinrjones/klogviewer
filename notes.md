## App updates

1. I want to watch a directory and load all the logs in the directory (and sub directories)
1. We don't need the log levels in the sidebar and the nav bar - in fact the whole UI needs an enema
   1. log lines should not wrap, extend the line off the screen and implement horizontal scrolling
   2. log lines should be closer together, make the gap smaller
   3. we don't need the filters on the sidepanel and the toolbar, remove them from the toolbar
   4. the tab bar is too wide, narrow it
   5. Can we change the sidebar filters to show counts for each of the categories, so '5 DEBUG' say
   6. Dark mode is too dark, how about dark grays rather than blacks  


1. Use multi item search - can search on multiple values and show each  one in a box that can be deleted to remove it 
from the search (like https://kittyfisto.github.io/Tailviewer/)
   


   
