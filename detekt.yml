build:
  maxIssues: 0
  excludeCorrectable: false
  # Generated outputs are excluded to keep findings focused on owned source files.
  excludes: '**/build/**,**/resources/**'

config:
  validation: true
  warningsAsErrors: false

processors:
  active: true

console-reports:
  active: true

output-reports:
  active: true
